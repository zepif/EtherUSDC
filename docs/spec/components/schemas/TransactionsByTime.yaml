allOf:
  - $ref: '#/components/schemas/TransactionsByTimeKey'
  - type: object
    required:
      - transactions
    properties:
      transactions:
        type: array
        items:
          $ref: '#/components/schemas/Transaction'

