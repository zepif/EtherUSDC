allOf:
  - $ref: '#/components/schemas/TransactionsListKey'
  - type: object
    required:
      - transactions
    properties:
      transactions:
        type: array
        items:
          $ref: '#/components/schemas/Transaction'

