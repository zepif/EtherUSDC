log:
  disable_sentry: true

db:
  url: postgres://...@.../...?sslmode=disable

listener:
  addr: :8000

cop:
  disabled: true
  endpoint: "http://..."
  upstream: "http://..."
  service_name: EtherUSDC
  service_port: 80

eth:
  EthRPC: "your eth rpc values"
  EthContractAddress: "your eth contract address value"
  EthContractABI: "your eth contract abi value"
